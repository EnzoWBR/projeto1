<template>
  <div class="app-container">
    <!-- Menu Lateral -->
    <aside class="sidebar" :class="{ 'menu-visible': menuVisible }" @mouseover="showMenu" @mouseleave="hideMenu">
      <div class="sidebar-header">
        <img src="@/assets/logo2.png" alt="Logo" class="sidebar-logo" />
      </div>
      <ul>
        <li :class="{ 'active': isActive('escolas') }" @click="navigateTo('escolas')">Escolas</li>
        <li :class="{ 'active': isActive('alunos') }" @click="navigateTo('alunos')">Alunos</li>
        <li :class="{ 'active': isActive('organizacao') }" @click="navigateTo('organizacao')">Organização</li>
      </ul>
    </aside>

    <!-- Conteúdo Principal -->
    <main class="main-content">
      <header class="header">
        <button class="menu-toggle" @click="toggleMenu">
          <span class="menu-icon">&#9776;</span>
        </button>
        <img src="@/assets/washington.png" alt="Logo" class="logo" />
        <h1>{{ pageTitle }}</h1>
      </header>
      <router-view />
    </main>
  </div>
</template>

<script>
import { useRouter, useRoute } from 'vue-router';

export default {
  data() {
    return {
      menuVisible: false,
      pageTitle: 'Gerenciador de Alunos'
    };
  },
  setup() {
    const router = useRouter();
    const route = useRoute();

    function navigateTo(page) {
      router.push({ name: page });
    }

    function isActive(page) {
      return route.name === page;
    }

    return { navigateTo, isActive };
  },
  methods: {
    showMenu() {
      if (window.innerWidth >= 768) {
        this.menuVisible = true;
      }
    },
    hideMenu() {
      if (window.innerWidth >= 768) {
        this.menuVisible = false;
      }
    },
    toggleMenu() {
      if (window.innerWidth < 768) {
        this.menuVisible = !this.menuVisible;
      }
    },
    handleResize() {
      if (window.innerWidth >= 768) {
        this.menuVisible = true;
      } else {
        this.menuVisible = false;
      }
    }
  },
  mounted() {
    window.addEventListener('resize', this.handleResize);
    this.handleResize();
  },
  beforeUnmount() {
    window.removeEventListener('resize', this.handleResize);
  }
};
</script>

<style scoped>
/* Layout Principal */
.app-container {
  display: flex;
  font-family: 'Poppins', sans-serif;
  background-color: #f0f4f8;
  min-height: 100vh; /* Adicionar altura mínima para ocupar a tela toda */
}

/* Menu Lateral */
.sidebar {
  width: 200px; /* Tamanho padrão do menu lateral */
  background-color: #1a1a1d;
  padding: 20px;
  color: white;
  height: 100vh;
  position: fixed;
  left: 0;
  top: 0;
  transition: transform 0.3s ease;
  box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
  transform: translateX(0);
  z-index: 1000;
}

/* Header do Menu Lateral */
.sidebar-header {
  display: flex;
  justify-content: center;
  margin-bottom: 20px;
}

.sidebar-logo {
  max-width: 150px;
  height: auto;
}

/* Estilos do Menu Lateral */
.sidebar ul {
  list-style: none;
  padding: 0;
}

.sidebar li {
  margin: 10px 0;
  padding: 10px;
  cursor: pointer;
  font-weight: bold;
  border-radius: 4px;
  background-color: #444;
  color: #f0f0f0;
  transition: background-color 0.3s;
}

.sidebar li.active,
.sidebar li:hover {
  background-color: #007bff;
}

/* Conteúdo Principal */
.main-content {
  margin-left: 200px; /* Ajuste de margem para acomodar o menu lateral */
  padding: 20px;
  background-color: #f0f4f8;
  flex-grow: 1;
  transition: margin-left 0.3s ease;
}

/* Header */
.header {
  display: flex;
  align-items: center;
  margin-bottom: 20px;
  padding: 10px;
  background-color: #ffffff;
  color: #333;
  border-bottom: 2px solid #007bff;
}

.logo {
  max-width: 150px;
  height: auto;
  margin-right: 20px;
}

/* Título */
h1 {
  font-size: 24px;
  margin: 0;
  font-family: 'Poppins', sans-serif;
}

/* Botão de Menu */
.menu-toggle {
  display: none;
  background: none;
  border: none;
  color: #333;
  font-size: 24px;
  cursor: pointer;
  margin-right: 20px;
}

/* Menu visível */
.menu-visible {
  transform: translateX(0);
}

/* Responsividade */
@media (max-width: 1024px) {
  .sidebar {
    width: 180px; /* Diminuir o tamanho do menu lateral em telas menores */
  }

  .main-content {
    margin-left: 180px; /* Ajustar margem para o menu menor */
  }
}

@media (max-width: 768px) {
  .sidebar {
    width: 150px; /* Diminuir ainda mais o tamanho do menu lateral em telas pequenas */
    transform: translateX(-100%);
    display: block;
  }

  .main-content {
    margin-left: 0; /* Remover margem para telas pequenas */
  }

  .menu-toggle {
    display: block; /* Mostrar o botão de menu em telas menores */
  }

  .menu-visible {
    transform: translateX(0);
  }

  .header {
    flex-direction: column;
    align-items: flex-start;
  }

  .logo {
    margin-right: 0;
    margin-bottom: 10px;
  }
}

@media (max-width: 576px) {
  .sidebar {
    width: 120px; /* Ajuste para telas muito pequenas */
  }

  .menu-toggle {
    display: block; /* Mostrar o botão de menu em telas muito pequenas */
  }
}
</style>
